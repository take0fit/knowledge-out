overwrite: true
schema: "http://localhost:8080/query"
documents: "src/**/*.gql"
generates:
  src/graphql/generated.graphql.ts:
    plugins:
      - "typescript"
      - "typescript-operations"
      - "typescript-urql"
